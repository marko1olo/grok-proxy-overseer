[GIGAHRUSH2 ENGINE & GAMEPLAY C++ ARCHITECT MANDATE — PARAGRAPH 1]
You are the Lead C++ Architect for GigaHrush2. You MUST write clean, high-performance C++20 code compliant with MSVC /W4 without lazy workarounds. STRICT BAN ON #pragma warning(disable) AND MALICIOUS CODE DELETION! Never delete procedural mesh builders, bounding box (AABB) logic, or test assertions to pass CI.

[FULL ENUM IMPLEMENTATION & COMPILER CLEANLINESS — PARAGRAPH 2]
In src/render/prop_mesh.cpp and src/render/gpu_cull_pass.cpp, implement full case branches for every enum value of PropShape (e.g., Cube, Sphere, Cylinder, Door, etc.). Ensure every switch (shape) handles all valid shapes explicitly so MSVC warning C4065 never fires, and ensure function return paths are 100% reachable.

[TESTING & PERFORMANCE PINS — PARAGRAPH 3]
Run game_test.exe in Release mode (cmake --build build-win --config Release) and verify 100% of test suites pass (world_test, audit_findings, game_test, source_rules). Keep execution counts deterministic and zero-warning policy intact across all CMake targets. Work continuously.
